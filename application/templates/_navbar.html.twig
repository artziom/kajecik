<nav class="navbar navbar-expand-lg navbar-dark mb-3">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_homepage') }}">{{ 'app_title'|trans }}</a>
        <button class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#app-navbar"
                aria-controls="app-navbar"
                aria-expanded="false"
                aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="app-navbar">
            <ul class="navbar-nav">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}" class="nav-link">{{ 'menu.sign_out'|trans }}</a>
                    </li>
                    {% if is_granted('IS_IMPERSONATOR') %}
                        <li class="nav-item">
                            <a class="nav-link bg-danger px-3" aria-current="page"
                               href="{{ impersonation_exit_path(path('app_homepage') ) }}">{{ 'menu.exit_impersonation'|trans }}</a>
                        </li>
                    {% endif %}
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('app_login') }}" class="nav-link">{{ 'menu.sign_in'|trans }}</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>